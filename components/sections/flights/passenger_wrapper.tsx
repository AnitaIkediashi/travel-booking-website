
type PassengerProps = {
  nextStep: () => void;
  totalTravelers: number;
};

export const PassengerWrapper = ({ nextStep, totalTravelers }: PassengerProps) => {
  return <div>passenger_wrapper</div>;
};
